name: CI Core

on:
  push:
    branches:
      - core
  pull_request:
    branches:
      - core

jobs:
  ci:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: CI sanity check
        run: |
          echo "Repo Cloned Successfully"
          ls -la
